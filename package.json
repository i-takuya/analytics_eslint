{
  "name": "eslint_test",
  "version": "1.0.0",
  "description": "test for eslint",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "./node_modules/.bin/eslint --ext \".js\" .",
    "fix": "./node_modules/.bin/eslint --fix --ext \".js\" ."
  },
  "author": "takinaga",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^4.13.1",
    "github": "^13.0.1"
  },
  "dependencies": {
    "co": "^4.6.0",
    "request-promise": "^4.2.2"
  }
}
